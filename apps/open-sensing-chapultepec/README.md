# open-sensing-chapultepec

Bitácora de observación medioambiental — a participatory weather-glyph map.
UI follows the "Bitacora UI/UX" Figma file.

```bash
yarn workspace @mono/open-sensing-chapultepec dev
```

Routes: `/` (map), `/compartir-glifo`, `/clima-hoy`, `/datos`, `/acerca`.
