# diario-fotos

Spec-only app (see docs/SPEC-FORMAT.md, "Four apps from JSON only"): `app/app.json` + `app/specs/*.json`, no components, handlers or stores.

```bash
yarn workspace @mono/base verify --app apps/diario-fotos
yarn workspace @mono/diario-fotos dev
```
